<h1>Articles</h1>

<%= link_to 'New Article', new_article_path%>

<ul>
    <% @articles.each do |article|%>
        <li>
            <a href="<%= article_path(article) %>"> <!--  <a href="/articles/<%= article.id %>">-->

                <%=link_to(article.title, article)%>
            </a>
        </li>
    <% end %>
</ul>